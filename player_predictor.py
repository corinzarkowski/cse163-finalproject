import os
import requests
import re
import pandas as pd

# if os.argument == -refresh-data or datafiles not exist:
#     init_data()

# check validity of college player
# if not valid throw error

# train random forest on data files
# return predictions for player